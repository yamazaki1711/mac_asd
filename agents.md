# AGENTS.MD — Манифест Оркестратора АСД v11.0 (Полный Жизненный Цикл)

> Этот файл — главная инструкция для Hermes (PM-агента) по управлению субагентами.
> Hermes читает этот файл при каждом запуске для определения логики маршрутизации.

---

## 1. Реестр агентов (7 специализированных ролей)

| #  | Агент             | Роль                    | Модель                          | Движок | Память ~ |
|----|-------------------|-------------------------|---------------------------------|--------|----------|
| 1  | **Hermes**        | Руководитель проекта    | Llama 3.3 70B Instruct 4bit     | MLX    | 38 GB    |
| 2  | **ПТО**           | Инженер ПТО (Vision)    | Gemma 4 31B 4-bit               | MLX    | 18 GB    |
| 3  | **Сметчик**       | Инженер-сметчик         | Qwen3 32B                       | MLX    | 18 GB    |
| 4  | **Юрист**         | Строительный юрист      | Qwen3 32B (DeepSeek fallback)   | MLX    | 0 GB*    |
| 5  | **Закупщик**      | Тендер-менеджер         | Qwen3 32B                       | MLX    | 0 GB*    |
| 6  | **Логист**        | Агент по логистике      | Qwen3 32B                       | MLX    | 0 GB*    |
| 7  | **Делопроизводитель**| Архивариус          | Gemma 4 9B                      | MLX    | 6 GB     |

> * Юрист, Закупщик и Логист используют ту же модель Qwen3, что и Сметчик — одна копия в памяти (Shared Memory).
> * Юрист: fallback на DeepSeek v3 (Ollama) для сложных юркейсов.

| Агент             | config.yaml           | prompt.md            | Группа инструментов MCP   |
|-------------------|-----------------------|----------------------|---------------------------|
| Hermes            | `agents/pm/`          | `agents/pm/`         | graph, task, artifact     |
| ПТО               | `agents/pto/`         | `agents/pto/`        | vision, graph, pto        |
| Сметчик           | `agents/smeta/`       | `agents/smeta/`      | smeta, graph              |
| Юрист             | `agents/legal/`       | `agents/legal/`      | legal, rag, graph         |
| Закупщик          | `agents/procurement/` | `agents/procurement/`| procurement, rag, graph   |
| Логист            | `agents/logistics/`   | `agents/logistics/`  | logistics, graph          |
| Делопроизводитель | `agents/archive/`     | `agents/archive/`    | archive, graph            |

---

## 2. Протокол "Полный Жизненный Цикл ОКС" (Event-Driven Workflow)

Система управляется событиями (Events). Каждый агент подписан на определенные триггеры.

### 2.1. ЭТАП 1: Предконтрактная подготовка (Тендер)

**Триггер:** Плановый запуск Закупщика
1. **Закупщик** находит лот, проверяет лимита НМЦК (по прошлым КС-11 из БД и MaterialCatalog), скачивает архивы (ПД, ВОР, ЛСР).
2. **Делопроизводитель** распаковывает архивы и структурирует файлы в `ЗАКУПКИ/{project_id}/`.
3. **ПТО** анализирует чертежи, извлекает спецификации материалов. Возвращает проверенную ВОР.
4. **Логист** берет спецификацию от ПТО, находит поставщиков (арматура, шпунт и т.д.), выбирает 3 лучших КП.
5. **Сметчик** и **Юрист** ПАРАЛЛЕЛЬНО проверяют ВОР/сметы (на рентабельность) и Контракт (на БЛС ловушки).
6. **Hermes** формирует итоговый вердикт: "Участвуем" или "Отказ".

### 2.2. ЭТАП 2: Снабжение и Входной контроль

**Триггер:** Событие `ProjectWon` (Выигрыш тендера)
1. **Логист** оформляет финальные заявки выбранным поставщикам, контролирует поступление ТТН и сертификатов качества.
2. **Делопроизводитель** вносит сертификаты и паспорта качества в базу (инструмент `asd_register_document`).
3. **ПТО** автоматически генерирует "Акт входного контроля".

### 2.3. ЭТАП 3: Производство и Исполнительная документация (ИД)

**Триггер:** Событие `OJPEntryClosed` (В ОЖР закрыта запись о выполнении работ)
1. **ПТО** получает сигнал. Ищет связанные сертификаты материалов (от Логиста) и чертежи РД.
2. **ПТО** вызывает `asd_generate_act` для создания АОСР (Акта скрытых работ).
3. **Делопроизводитель** присваивает АОСР номер и сохраняет в реестр ИД.

### 2.4. ЭТАП 4: Коммерческое закрытие (Ежемесячно)

**Триггер:** Конец отчетного расчетного периода
1. **Сметчик** собирает все подписанные АОСР за месяц. Сверяет со сметой (`asd_estimate_compare`).
2. **Сметчик** ведет журнал КС-6а, формирует заготовки актов КС-2 и КС-3.
3. **Делопроизводитель** формирует опись для отправки (`asd_prepare_shipment`).

### 2.5. ЭТАП 5: Претензии и Завершение

**Триггер:** Событие `DeadlineExpired` (Нарушение сроков оплаты или ответа)
1. **Юрист** генерирует досудебную претензию (`asd_generate_claim`).
2. **Юрист** формирует исковое заявление (`asd_generate_lawsuit`) при необходимости.
3. **ПТО** собирает итоговый реестр ИД (`asd_id_completeness`) для подписания КС-11.

---

## 3. Обработка ошибок в конвейере

| Ситуация                           | Действие Hermes / Hệ thống                                 |
|------------------------------------|------------------------------------------------------------|
| Нет сертификата на материал в БД   | Делопроизводитель блокирует создание АОСР, уведомляет РП.  |
| Смета ≠ ВОР                        | Конфликт фиксируется, Сметчик возвращает задачу в ПТО.     |
| Юрист нашёл критическую ловушку    | Остановка подписания контракта (Уровень риска: HIGH).      |
| Логист не нашел КП с нужной ценой  | Эскалация на Hermes для пересмотра бюджета или отказа.     |
| Субагент вернул пустой результат   | Retry (max 2), затем эскалация на человека.                |
| ПТО не нашел шпунт на чертеже      | Пометка "шпунт не обнаружен — требует ручной проверки".    |

---

## 4. Хранение состояния

* **NetworkX** — граф проектов, документов, событий (in-memory + файловая сериализация в data/graphs/).
* **Redis** — кеш результатов Vision ПТО, rate-lookups, RAG-queries (TTL 24h).
* **LLMEngine** — единый интерфейс к LLM с поддержкой профилей dev_linux/mac_studio.
* **Artifact Store** — файлы сохраняются в `./data/artifacts/`.
* **LightRAG** — векторные индексы: `legal_rag`, `rates_rag`, `vendor_rag` (для Логиста).
* **Registry** — `data/artifacts/{project_id}/registry.json`

Все данные RAG (LightRAG) и связи (NetworkX) хранятся в `data/` (графы сериализуются в `data/graphs/`).

---

## 5. Технические регламенты

* **MLX / Ollama** — модели работают через LLMEngine с профилями (ASD_PROFILE=mac_studio для MLX, dev_linux для Ollama).
* **Ollama** — бэкенд для dev_linux профиля и embeddings (bge-m3) на mac_studio.
* **Shared models:** Qwen3 32B — одна физическая копия в VRAM для Сметчика, Юриста, Логиста и Закупщика.

---

## 6. Правила Orchestrator'а (Hermes)

1. **Конвейер событий (Event Bus):** Hermes работает как слушатель событий и диспетчер, а не как жесткий скрипт.
2. **Логирование:** Каждое действие агента (созданный АОСР, полученное КП) = запись в граф БД с timestamp.
3. **Идемпотентность:** Повторная генерация документа (например, при правках) не дублирует узлы, а версионирует их.
4. **Человек в контуре:** Прохождение ключевых "ворот" (Gateways) ПТО -> КС-2 -> Оплата требует верификации человеком-руководителем.
5. **Профилирование:** Все LLM-вызовы проходят через LLMEngine, который выбирает бэкенд (Ollama/MLX) по текущему профилю ASD_PROFILE.
