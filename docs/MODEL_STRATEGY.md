# АСД v11.0 — МОДЕЛЬНАЯ СТРАТЕГИЯ И УПРАВЛЕНИЕ ПАМЯТЬЮ

**Дата:** 17 апреля 2026
**Статус:** Активная разработка (Package 1 завершен, LLMEngine реализован)
**Платформа:** Mac Studio M4 Max 128GB Unified Memory

---

## 1. ОБЗОР

Стратегия использования моделей Gemma 4 на Apple Silicon:

- **Приоритет: FP16** — качество важнее скорости. Человек тратит неделю на анализ ПД, АСД справится за часы. Нет смысла жертвовать точностью ради скорости.
- Какие модели загружены когда
- Как распределяется 128GB памяти
- Когда включается thinking mode
- Как предотвращать OOM

---

## 2. МОДЕЛИ

### 2.1. Llama 3.3 70B — Оркестратор (Hermes)

| Параметр | Значение |
|----------|----------|
| **Параметров** | 70 млрд |
| **Квантизация** | 4-bit |
| **RAM** | **~38 GB** |
| **Контекст** | до 128K tokens |
| **Формат** | MLX / GGUF |
| **Бэкенд** | MLX (mac_studio) / Ollama (dev_linux) |
| **Скорость** | ~3-7 tokens/sec |
| **Роль** | Маршрутизация, координация, вердикты |

**Почему Llama 3.3 70B как оркестратор:**

- Наиболее мощная модель — отвечает за маршрутизацию запросов и финальные вердикты
- Управляет потоком задач между агентами
- Качество принятия решений критично для всей системы
- Всегда загружена — ядро системы

### 2.1a. Qwen3 32B — Универсальная рабочая модель

| Параметр | Значение |
|----------|----------|
| **Параметров** | 32 млрд |
| **Квантизация** | 4-bit |
| **RAM** | **~18 GB** |
| **Формат** | MLX / GGUF |
| **Бэкенд** | MLX (mac_studio) / Ollama (dev_linux) |
| **Роль** | Сметчик, Юрист, Закупщик, Логист (shared memory — одна копия) |

**Ключевое преимущество — экономия RAM:**

- Одна модель Qwen3 32B обслуживает 4 агента через shared memory
- Вместо 4 отдельных моделей × 18 GB = 72 GB → всего 18 GB
- **Экономия ~54 GB RAM** по сравнению с отдельными моделями для каждого агента
- Переключение между агентами через системный промпт, без перезагрузки модели

### 2.2. Gemma 4 31B — Vision / ПТО-агент

| Параметр | Значение |
|----------|----------|
| **Параметров** | 31 млрд |
| **Квантизация** | 4-bit |
| **RAM** | **~18 GB** |
| **Формат** | MLX / GGUF |
| **Бэкенд** | MLX (mac_studio) / Ollama (dev_linux) |
| **Роль** | ПТО-агент и vision-задачи (анализ чертежей, сканов, схем) |

**Используется исключительно для ПТО и vision:**

- Анализ проектной документации с визуальными компонентами
- Распознавание и интерпретация чертежей и схем
- Проверка соответствия проектных решений нормативам
- Vision-задачи, требующие понимания изображений

### 2.3. Gemma 4 9B — Делопроизводитель (лёгкая LLM)

| Параметр | Значение |
|----------|----------|
| **Параметров** | ~9 млрд |
| **Квантизация** | 4-bit |
| **RAM** | ~6 GB |
| **Формат** | GGUF |
| **Бэкенд** | Metal |
| **Скорость** | ~40-80 tokens/sec |

### 2.4. bge-m3 — embeddings

| Параметр | Значение |
|----------|----------|
| **Размерность** | 1024 |
| **RAM** | ~2.2 GB |
| **Языки** | 100+ (включая русский) |
| **Скорость** | ~100 текстов/сек |

### 2.5. minicpm-v — vision/OCR

| Параметр | Значение |
|----------|----------|
| **Параметров** | ~8 млрд |
| **Квантизация** | Q4_K_M |
| **RAM** | ~5 GB |
| **Назначение** | Распознавание сложных сканов с таблицами |

---

## 3. RAM БЮДЖЕТ (128GB)

### 3.1. Базовая конфигурация (модели загружены)

| Компонент | RAM | Примечание |
|-----------|-----|------------|
| Llama 3.3 70B 4-bit (Hermes) | 38.0 GB | Всегда загружена |
| Qwen3 32B 4-bit (4 агента) | 18.0 GB | Shared Memory |
| Gemma 4 31B 4-bit (ПТО) | 18.0 GB | Всегда загружена |
| bge-m3 | 2.2 GB | Всегда загружена |
| **Итого модели** | **76.2 GB** | |
| macOS + PostgreSQL + App | 18.0 GB | |
| **Занято базово** | **94.2 GB** | |
| **Свободно** | **33.8 GB** | На контекст и пики |

> **Примечание:** Gemma 4 9B (6 GB) подгружается по необходимости для Делопроизводителя.

### 3.2. Контекст (переменный)

| Операция | Доп. RAM | Длительность |
|----------|----------|--------------|
| Анализ чанка (31B FP16) | +5-10 GB | 20-40 сек |
| Map-Reduce (последовательно) | +5-10 GB (пик) | ~20 сек/чанк |
| Генерация DOCX | +1-2 GB | 10-30 сек |
| OCR скана (minicpm-v, подгружается) | +5 GB | 3-10 сек/стр |
| Подгрузка E4B | +2.5 GB | ~30 сек |

### 3.3. Пиковая нагрузка

Типичный сценарий: последовательный Map-Reduce анализа 650 чанков ПД

- подгруженный minicpm-v для OCR + генерация отчёта:

```
Базово (FP16):     82.2 GB
Map-Reduce пик:    10.0 GB (последовательно, один чанк)
OCR (minicpm-v):    5.0 GB (подгружается по необходимости)
Генерация:          2.0 GB
─────────────────────────
Пик:               99.2 GB
Остаётся:          28.8 GB
```

**Важно:** Map-Reduce работает **последовательно** (не параллельно).
Нет пика от 3 одновременных задач. RAM растёт плавно.

### 3.4. Альтернатива: Q8_0 при нехватке памяти

Когда нужно освободить RAM (давление > high):

```
gemma-4-31b:q8_0   33.0 GB  ← экономия 29 GB
bge-m3              2.2 GB
Система            18.0 GB
─────────────────────────
Базово:            53.2 GB
Контекст пик:      10.0 GB
Остаётся:          64.8 GB  ← запас для параллельных задач

E4B и minicpm-v подгружаются по необходимости.
Q8_0 — это emergency fallback, не стандарт.
```

---

## 4. УРОВНИ ДАВЛЕНИЯ ПАМЯТИ

Базовая загрузка при FP16: ~82 GB (64%). Поэтому пороги смещаются.

### 4.1. low (< 70% использовано, < 90 GB)

- FP16 31B + bge-m3 загружены
- Последовательная обработка (без параллелизма)
- minicpm-v и E4B подгружаются по необходимости, выгружаются после использования

### 4.2. medium (70-80%, 90-102 GB)

- FP16 31B остаётся
- Нормально: пик контекста при Map-Reduce укладывается
- Предупреждение в логах при приближении к 80%

### 4.3. high (80-90%, 102-115 GB)

- Все дополнительные модели (E4B, minicpm-v) немедленно выгружаются
- Оставшиеся задачи завершаются последовательно
- Предупреждение пользователю: "Высокое использование памяти"
- Новые задачи ставятся в очередь

### 4.4. critical (> 90%, > 115 GB)

- Переключение на Q8_0 (экономия 29 GB)
- Все дополнительные модели выгружаются
- Новые задачи отклоняются с ошибкой `MEMORY_CRITICAL`
- Пользователь: "Критическое использование памяти. Подождите завершения текущих задач."

---

## 5. МОДЕЛЬНЫЙ РОУТИНГ

### 5.1. Таблица маршрутизации

```
Агент               → Модель              │ Бэкенд
─────────────────────────────────────────────────────
pm (Руководитель)   → Llama 3.3 70B       │ MLX / Ollama
pto (ПТО)           → Gemma 4 31B         │ MLX / Ollama
smeta (Сметчик)     → Qwen3 32B (shared)  │ MLX / Ollama
legal (Юрист)       → Qwen3 32B (shared)  │ MLX / Ollama
procurement         → Qwen3 32B (shared)  │ MLX / Ollama
 (Закупщик)         │
logistics (Логист)  → Qwen3 32B (shared)  │ MLX / Ollama
archive             → Gemma 4 9B          │ MLX / Ollama
 (Делопроизводитель)│                      │
```

> **Примечание:** Маршрутизация реализована через LLMEngine: `settings.get_model_config(agent_name)` автоматически определяет модель и бэкенд по текущему профилю.

### 5.2. Fallback-цепочки

```
Llama 3.3 70B недоступна
  → Qwen3 32B (сниженное качество маршрутизации)
    → Gemma 4 31B (минимальный режим)
      → Ошибка "Оркестратор недоступен"

Qwen3 32B недоступна
  → Llama 3.3 70B (сниженное качество, повышенный расход RAM)
    → Ошибка "Рабочая модель недоступна"

Gemma 4 31B (ПТО) недоступна
  → Llama 3.3 70B (fallback для ПТО)
    → Ошибка "ПТО-агент недоступен"

bge-m3 недоступна
  → Ошибка "Поиск временно недоступен"
  → Fallback: полнотекстовый поиск (pg_trgm) без embeddings

minicpm-v недоступен
  → pytesseract (базовый OCR)
    → Ошибка "OCR недоступен"
```

---

## 6. MAP-REDUCE СТРАТЕГИЯ

### 6.1. Почему Map-Reduce?

Gemma 4 31B имеет контекст до 128K tokens (~96K слов, ~500 страниц).
Но:

1. **Качество:** Модель лучше анализирует чанки среднего размера (6000 tokens) — видит больше контекста, чем 4000, но не перегружена
2. **Память:** При FP16 большой контекст = большой расход RAM. Последовательная обработка минимизирует пик
3. **Рекурсивный Reduce:** Многораундовая агрегация даёт более точный финальный результат

### 6.2. Параметры

| Параметр | Значение | Почему |
|----------|----------|--------|
| Chunk size | **6000 tokens** | Больше контекста для FP16, точнее анализ юридических конструкций |
| Overlap | **300 tokens** | Больше overlap — меньше потерь на границах |
| Max parallel | **1** | Последовательная обработка, минимальный пик RAM |
| Timeout на чанк | 300 сек | 5 мин на чанк |
| Max retries | 2 | При ошибке LLM |
| Reduce rounds | **3** | Map → Reduce1 (×10) → Reduce2 (×10) → Final |

### 6.3. Расчёт времени

```
Договор 150 страниц:
  → ~185,000 слов
  → ~31 чанк (по 6000 tokens)
  → Последовательно: 31 × 25 сек = ~13 мин (Map)
  → Reduce (3 раунда): 31 → 4 → 1 = ~3 мин
  → Итого: ~16 мин

ПД 2450 страниц:
  → ~3,000,000 слов
  → ~500 чанков (по 6000 tokens)
  → Последовательно: 500 × 25 сек = ~208 мин ≈ 3.5 часа (Map)
  → Reduce (3 раунда): 500 → 50 → 5 → 1 = ~15 мин
  → Итого: ~3.5-4 часа

Человек: 5 дней × 3 человека = 15 человеко-дней
АСД: 3.5-4 часа непрерывной работы
Ускорение: ~40-50×
```

---

## 7. OLLAMA КОНФИГУРАЦИЯ

### 7.1. Modelfile для Gemma 4 31B (FP16 — стандарт)

```
FROM gemma-4-31b:fp16

PARAMETER num_ctx 16384        # Увеличен для лучшего контекста
PARAMETER temperature 0.3
PARAMETER num_gpu 99           # Использовать все GPU слои (Metal)
PARAMETER num_thread 12        # Половина ядер M4 Max
```

### 7.2. Modelfile для Gemma 4 31B (Q8_0 — emergency fallback)

```
FROM gemma-4-31b:q8_0

PARAMETER num_ctx 8192
PARAMETER temperature 0.3
PARAMETER num_gpu 99
PARAMETER num_thread 12
```

### 7.3. Modelfile для Gemma 4 E4B

```
FROM gemma-4-e4b:q4_k_m

PARAMETER num_ctx 4096
PARAMETER temperature 0.1
PARAMETER num_gpu 99
PARAMETER num_thread 8
```

### 7.4. Загрузка моделей

```bash
# Основные (всегда загружены)
ollama pull gemma-4-31b:fp16    # ← FP16 — стандарт
ollama pull bge-m3

# Подгружаются по необходимости (ollama pull → ollama stop после использования)
ollama pull gemma-4-e4b:q4_k_m
ollama pull minicpm-v:q4_k_m

# Emergency fallback (если нужен Q8_0)
ollama pull gemma-4-31b:q8_0
```

### 7.5. Ollama серверные настройки

```bash
# /etc/launchd.conf или переменные окружения
OLLAMA_HOST=0.0.0.0
OLLAMA_PORT=11434
OLLAMA_MODELS=/Users/oleg/.ollama/models
OLLAMA_NUM_PARALLEL=1            # Последовательная обработка
OLLAMA_MAX_LOADED_MODELS=3       # До 3 моделей одновременно
```

### 7.6. MLX конфигурация (mac_studio)

На Mac Studio все тяжёлые LLM-модели работают через **MLX** бэкенд:

- Llama 3.3 70B — MLX (максимальная производительность на Apple Silicon)
- Qwen3 32B — MLX
- Gemma 4 31B — MLX
- Gemma 4 9B — MLX (подгружается по необходимости)

Ollama на Mac Studio используется **только для embeddings** (bge-m3).

LLMEngine автоматически определяет бэкенд на основе переменной окружения `ASD_PROFILE`:

- `ASD_PROFILE=mac_studio` → LLM через MLX, embeddings через Ollama
- `ASD_PROFILE=dev_linux` → все модели через Ollama

Конфигурация моделей и бэкендов задана в `settings.MODEL_CONFIGS` и выбирается через `settings.get_model_config(agent_name)`.

---

## 8. МОНИТОРИНГ ПАМЯТИ

### 8.1. Метрики

```python
RAMManager.get_metrics() → {
    "total_gb": 128.0,
    "used_gb": 90.5,               # FP16 31B = 62 GB базово
    "available_gb": 37.5,
    "pressure": "low",             # low | medium | high | critical
    "model_quality": "fp16",       # fp16 | q8_0 | e4b
    "models_loaded": ["gemma-4-31b:fp16", "bge-m3"],
    "models_memory": {
        "gemma-4-31b:fp16": 62.0,
        "bge-m3": 2.2
    },
    "active_tasks": 1,             # Последовательная обработка
    "peak_memory_gb": 99.2,
    "last_gc_time": "2026-04-13T10:30:00Z"
}
```

### 8.2. Частота проверки

| Событие | Действие |
|---------|----------|
| Каждая задача | Проверка перед запуском |
| Каждые 30 сек | Фоновая проверка |
| Завершение задачи | Пересчёт доступной памяти |
| Давление = high | Предупреждение в лог |
| Давление = critical | Отказ новых задач |

---

## 9. ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ

### 9.1. Кэширование

| Что кэшировать | Где | TTL |
|----------------|-----|-----|
| Embeddings чанков | PostgreSQL (vector column) | Постоянно |
| Результаты поиска | Redis / in-memory | 5 мин |
| Ответы LLM (одинаковые запросы) | in-memory | 1 мин |
| Статус системы | in-memory | 30 сек |

### 9.2. Батчинг embeddings

Вместо последовательных запросов:

```python
# Плохо: 500 запросов по 300 мс = 150 сек
for chunk in chunks:
    embedding = ollama.embeddings("bge-m3", chunk.text)

# Хорошо: батч по 32 = ~16 запросов по 1 сек = ~16 сек
batch_size = 32
for i in range(0, len(chunks), batch_size):
    batch = chunks[i:i+batch_size]
    embeddings = ollama.batch_embeddings("bge-m3", [c.text for c in batch])
```

### 9.3. Последовательный Map-Reduce

```python
# Последовательная обработка (FP16, минимальный пик RAM)
async def analyze_chunk(chunk):
    return await ollama.chat("gemma-4-31b:fp16", messages, thinking=True)

# Без параллелизма — один чанк за раз
results = []
for chunk in chunks:
    result = await analyze_chunk(chunk)
    results.append(result)
    # RAMManager.check() после каждого чанка
    if RAMManager.should_unload():
        await ollama.stop("minicpm-v")  # освободить память
```

---

Этот документ определяет стратегию использования моделей и управления памятью.
Стратегия реализована в LLMEngine (src/core/llm_engine.py) с поддержкой профилей.
